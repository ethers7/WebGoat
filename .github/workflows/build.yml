name: "Main / Pull requests build"
on:
    pull_request:
        paths-ignore:
            - '.txt'
            - 'LICENSE'
            - 'docs/**'
        branches: [ main ]
    push:
        branches:
            - main

jobs:
    pre-commit:
        name: Pre-commit check
        runs-on: ubuntu-latest
        steps:
            -   name: Checkout git repository
                # TODO: pin to a specific commit SHA for supply chain security, e.g. uses: actions/checkout@<sha> # v7
                uses: actions/checkout@v7
            -   name: Setup python
                # TODO: pin to a specific commit SHA for supply chain security, e.g. uses: actions/setup-python@<sha> # v7
                uses: actions/setup-python@v7
                with:
                    python-version: "3.9"
            -   name: Setup Java
                uses: ./.github/actions/java-setup
                with:
                    cache: ""
            -   name: Pre-commit checks
                # TODO: pin to a specific commit SHA for supply chain security, e.g. uses: pre-commit/action@<sha> # v3.0.1
                uses: pre-commit/action@v3.0.1
            -   name: pre-commit-c-lite
                # TODO: pin to a specific commit SHA for supply chain security, e.g. uses: pre-commit-ci/lite-action@<sha> # v1.1.0
                uses: pre-commit-ci/lite-action@v1.1.0
                if: always()
    build:
        runs-on: ${{ matrix.os }}
        needs: [ pre-commit ]
        strategy:
            fail-fast: true
            matrix:
                os: [ windows-latest, ubuntu-latest, macos-15-intel ]
            max-parallel: 1
        steps:
            # TODO: pin to a specific commit SHA for supply chain security, e.g. uses: actions/checkout@<sha> # v7
            -   uses: actions/checkout@v7
            -   name: Set up JDK
                uses: ./.github/actions/java-setup
                with:
                    cache: 'maven'
            -   name: Build with Maven
                run: mvn --no-transfer-progress verify
